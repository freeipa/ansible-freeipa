---
- name: Playbook to manage vault container.
  hosts: ipaserver
  become: no
  gather_facts: no

  tasks:
  - name: Check if shared vault container is present.
    ipavaultcontainer:
      ipaadmin_password: SomeADMINpassword
      shared: yes
